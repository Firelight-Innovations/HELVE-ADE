{
  "name": "helve-orchestrator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "dev": "vite",
    "dev:agent": "vite --mode agent",
    "build": "pnpm run build:bridge && pnpm run generate:icons && pnpm run generate:branding && pnpm run build:file-icons && pnpm run build:monaco-languages && tsc && vite build",
    "build:bridge": "pnpm --filter @helve/bridge build",
    "build:file-icons": "pnpm --filter @helve/file-icons build",
    "build:monaco-languages": "pnpm --filter @helve/monaco-languages build",
    "generate:icons": "node scripts/generate-file-icons.mjs",
    "generate:branding": "node scripts/generate-branding.mjs",
    "postinstall": "pnpm run generate:icons && pnpm run generate:branding",
    "lint": "pnpm run lint:branding && pnpm run lint:js && pnpm run lint:rust && pnpm run lint:comments",
    "lint:branding": "node scripts/check-branding.mjs",
    "lint:js": "eslint .",
    "lint:rust": "node scripts/clippy-baseline.mjs",
    "lint:comments": "node scripts/check-comments.mjs",
    "lint:deps": "cargo deny check",
    "slop": "node scripts/slop.mjs",
    "lint:fix": "eslint . --fix",
    "test": "pnpm run test:js && pnpm run test:rust",
    "test:js": "vitest run && pnpm --filter \"./packages/**\" --filter \"./examples/**\" --if-present test",
    "test:rust": "cargo test --workspace",
    "typecheck": "pnpm run build:bridge && pnpm run generate:branding && pnpm run build:file-icons && pnpm run build:monaco-languages && tsc",
    "verify": "pnpm build && pnpm test && pnpm lint && pnpm format:check",
    "verify:fast": "pnpm run typecheck && pnpm test && pnpm lint && pnpm format:check",
    "format": "prettier --write . && cargo fmt --all",
    "format:check": "prettier --check . && cargo fmt --all -- --check",
    "baseline": "eslint . --suppress-all && node scripts/clippy-baseline.mjs --update && node scripts/check-comments.mjs --update",
    "preview": "vite preview",
    "tauri": "tauri",
    "app": "tauri dev",
    "app:build": "tauri build"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "@helve/bridge": "workspace:*",
    "@helve/file-icons": "workspace:*",
    "@helve/monaco-languages": "workspace:*",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "framer-motion": "^13.1.0",
    "material-icon-theme": "^5.37.0",
    "mermaid": "^11.16.1",
    "monaco-editor": "^0.56.0",
    "pdfjs-dist": "^6.2.108",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^7.0.4",
    "vitest": "^4.1.10"
  }
}
