# Code owners. GitHub requests a review from the owner of every file a pull
# request touches, and the last matching pattern in this file wins.
#
# For a repository with one maintainer this is close to a no-op, and it is here
# anyway for the day it stops being one. The point is not the blanket rule at
# the top; it is the four paths below it, which are the ones where an accepted
# change is difficult to take back.
#
# ## Why a username and not a team
#
# This file named `@Firelight-Innovations/maintainers` until 2026-08-21, and
# that team does not exist — the organisation has no teams at all. **A
# CODEOWNERS rule naming an unresolvable owner matches nobody**, and GitHub does
# not fail the file or warn in the pull request, so every rule below was silently
# doing nothing for as long as it had been there.
#
# A username cannot be wrong in that way. Swap it back to a team when one exists
# and has members; until then a real owner beats a tidier-looking one.
#
# ## This only bites once review is required
#
# CODEOWNERS on its own *requests* a review. It blocks nothing until
# "Require review from Code Owners" is on in `main`'s branch protection, which
# it is not as of 2026-08-21 — `required_pull_request_reviews` is null.
#
# That setting is what makes this file a boundary rather than a convention, and
# it is the only boundary GitHub evaluates server-side. It matters most for
# `/.github/`: a `pull_request` workflow runs the pull request's own code, so a
# check living in this tree — `scripts/check-catalog.mjs`, say — can be edited
# by the very change it is checking. Nothing inside a branch can reach this
# rule, which is the whole point of it.

* @braden-seaborn

# The tool protocol is the contract the whole open-core model rests on. Every
# tool repository — first-party, commercial, and third-party alike — is written
# against this document, so a change here is a change to somebody else's
# software. It is called out separately from the rest of `docs/` for that
# reason, not because documentation is otherwise unowned.
/docs/tool-protocol.md @braden-seaborn

# The published surfaces the protocol document describes: the crates other
# repositories depend on, and the npm package a tool author actually installs.
# STANDARDS.md §10 already treats these differently from the application code —
# `missing_docs` and `unreachable_pub` are set here and nowhere else — for the
# same reason they are listed here.
/crates/ @braden-seaborn
/packages/bridge/ @braden-seaborn

# The rule book, and the machinery that enforces it. A pull request that relaxes
# a lint, widens a baseline, or edits the gate is a policy change wearing a
# configuration change's clothes.
/STANDARDS.md @braden-seaborn
/.github/ @braden-seaborn

# Schematify's design/audit split (docs/design/SCHEMATIFY-PRD.md §14.6, §6.2).
# `nodes/`, `edges/`, `rules/`, `screens/`, `flows/`, `decisions/`, `registry/`
# and `brief.json` are semantic — human-authored design, the thing an agent
# "shall not change" per §6.2. `runs/` and `layout/` are the two trees a machine
# writes instead: a CI job appends a benchmark result under `runs/`, and a node
# drag rewrites `layout/`. §14.6 is explicit that those two carry no owner "because
# a CI job and a node drag write them" — assigning a human owner to a tree only
# automation touches would make every routine write wait on a review it does not
# need. Section 6.3's one exception, the lifecycle pair, is a human-triggered
# transition and is still `nodes/` + `runs/`; it is `nodes/` that carries the
# owner in that pair, same as any other write there, and `runs/` still does not.
#
# No leading slash on any of these five lines, deliberately — unlike every rule
# above. `.kaava/` is a *project's* root, not a fixed location in this repository:
# today it exists three times, under
# `crates/schematify-core/fixtures/{dense-service,saas-backend,stress-2000}/`,
# and a project opened elsewhere in a checkout (including at the repository
# root, if this repository is ever opened as a Schematify project itself) would
# add another. An unanchored pattern matches `.kaava/nodes/` at any depth, the
# same way `apps/ @octocat` in GitHub's own CODEOWNERS documentation matches an
# `apps` directory anywhere rather than only at the root — so this does not need
# updating every time a fixture is added or renamed.
#
# These five lines come after `/crates/` above on purpose: CODEOWNERS resolves
# a path against the *last* matching line in the file, and `/crates/` alone
# would hand every one of these fixture trees to the owner, `runs/` and
# `layout/` included. Placing the two unowned lines below give them the last
# word for the paths they name.
.kaava/brief.json @braden-seaborn
.kaava/nodes/ @braden-seaborn
.kaava/edges/ @braden-seaborn
.kaava/rules/ @braden-seaborn
.kaava/screens/ @braden-seaborn
.kaava/flows/ @braden-seaborn
.kaava/decisions/ @braden-seaborn
.kaava/registry/ @braden-seaborn
.kaava/runs/
.kaava/layout/
